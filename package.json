{
  "name": "alliance-mutual-escrow",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Production website for Alliance Mutual Escrow, an independent DFPI licensed escrow company serving California.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node tests/regression.mjs",
    "test:build": "astro build && node tests/regression.mjs",
    "deploy": "astro build && node tests/regression.mjs && wrangler pages deploy dist --project-name alliance-mutual-escrow",
    "deploy:preview": "astro build && wrangler pages deploy dist --project-name alliance-mutual-escrow --branch preview"
  },
  "dependencies": {
    "@astrojs/react": "^4.2.0",
    "astro": "^5.4.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@fontsource/ibm-plex-mono": "^5.1.1",
    "@fontsource/inter": "^5.1.1",
    "@fontsource/instrument-serif": "^5.1.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "playwright": "^1.50.0",
    "wrangler": "^3.100.0"
  }
}
